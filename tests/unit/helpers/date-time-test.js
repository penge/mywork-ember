import { dateTime } from '../../../helpers/date-time';
import { module, test } from 'qunit';

module('Unit | Helper | date time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateTime(42);
  assert.ok(result);
});
